# === Production API URL ===
# Set this ONLY in your Netlify dashboard environment variables.
# Do NOT set this for local development (leave it blank locally so Vite's proxy works).
# VITE_API_URL=https://career-insider-7muf.onrender.com/api
